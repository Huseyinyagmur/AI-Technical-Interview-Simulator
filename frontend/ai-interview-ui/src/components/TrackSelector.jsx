export default function TrackSelector({ tracks, selected, onSelect }) { return <div className="track-grid">{tracks.map(track => <button type="button" key={track.id} className={`track-card ${selected === track.id ? 'selected' : ''}`} onClick={() => onSelect(track.id)}><strong>{track.title}</strong><span>{track.description}</span><small>{track.domains.join(' · ')}</small></button>)}</div>; }
