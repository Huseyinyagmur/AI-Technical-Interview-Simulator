import { useEffect, useState } from 'react';
import { getReport } from '../services/interviewApi';
export default function Report({ sessionId, onRestart, onDashboard }) { const [report, setReport] = useState(null); const [error, setError] = useState(''); useEffect(() => { getReport(sessionId).then(setReport).catch(e => setError(e.message)); }, [sessionId]); if (error) return <main className="page"><p className="error">{error}</p></main>; if (!report) return <main className="page">Rapor hazırlanıyor…</main>; return <main className="page"><section className="card"><p className="eyebrow">Profesyonel mülakat raporu · {report.topic}</p><h1>{report.averageScore}/100</h1><p>{report.interviewSummary}</p><p>{report.overallFeedback}</p><div className="report-grid"><p><strong>Güçlü alanlar</strong><br />{report.strongTopics?.join(', ') || 'Henüz belirlenmedi'}</p><p><strong>Gelişim alanları</strong><br />{report.weakTopics?.join(', ') || 'Henüz belirlenmedi'}</p><p><strong>Önerilen çalışma</strong><br />{report.recommendedTopics?.join(', ') || 'Genel tekrar'}</p><p><strong>Ulaşılan zorluk</strong><br />{report.difficulty}</p></div><button className="secondary" onClick={onDashboard}>Dashboard</button><button onClick={onRestart}>Yeni görüşme</button></section>{report.answers.map(x => <section className="card" key={x.questionNumber}><p className="eyebrow">Soru {x.questionNumber} · {x.concept} · {x.difficulty} · {x.evaluation.score}/100</p><h3>{x.question}</h3><p>{x.evaluation.improvementSuggestion}</p></section>)}</main>; }
