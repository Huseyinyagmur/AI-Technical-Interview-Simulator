import { useState } from 'react'; import { login } from '../services/authApi';
export default function Login({ onAuth, onRegister }) { const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState(''); const submit=async e=>{e.preventDefault();try{onAuth(await login(email,password));}catch(x){setError(x.message)}};return <main className="auth-page"><section className="auth-shell"><aside className="auth-aside"><div className="auth-logo">AI</div><p className="eyebrow">AI Technical Interview</p><h2>Görüşme pratiğini<br />ölçülebilir ilerlemeye dönüştür.</h2><div className="auth-aside-card"><span>✦</span><p>Track bazlı sorular, anlık değerlendirme ve kişisel çalışma planı.</p></div></aside><form className="auth-card" onSubmit={submit}><div className="auth-heading"><p className="eyebrow">Hoş geldin</p><h1>Giriş yap</h1><p>Görüşme geçmişine ve kişisel dashboard’una eriş.</p></div><label>Email adresi</label><input type="email" placeholder="ornek@email.com" value={email} onChange={e=>setEmail(e.target.value)} required/><label>Şifre</label><input type="password" placeholder="••••••••" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<p className="error">{error}</p>}<button className="auth-primary">Giriş yap <span>→</span></button><button type="button" className="auth-link" onClick={onRegister}>Hesabın yok mu? <b>Kayıt ol</b></button></form></section></main>; }
