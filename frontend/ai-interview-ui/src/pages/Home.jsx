import { useEffect, useState } from 'react';
import TopicSelector from '../components/TopicSelector';
import TrackSelector from '../components/TrackSelector';
import { getTracks, startInterview } from '../services/interviewApi';
export default function Home({ onStarted }) { const [topic, setTopic] = useState('C#'); const [difficulty, setDifficulty] = useState('Junior'); const [tracks, setTracks] = useState([]); const [track, setTrack] = useState('csharp-backend'); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); useEffect(() => { getTracks().then(setTracks).catch(() => setError('Track listesi alınamadı.')); }, []); const start = async e => { e.preventDefault(); setBusy(true); setError(''); try { onStarted(await startInterview(topic, difficulty, track)); } catch (err) { setError(err.message); } finally { setBusy(false); } }; return <main className="home-page"><section className="home-hero"><div className="brand-mark">AI</div><p className="eyebrow">AI Technical Interview Simulator</p><h1>Bir sonraki teknik görüşmene daha hazır gir.</h1><p className="hero-copy">Kariyer yolunu seç, seviyeni belirle ve gerçekçi sorularla pratik yap. Her cevap için net geri bildirim al.</p><div className="hero-points"><span>5 uyarlanabilir soru</span><span>Anlık geri bildirim</span><span>Kişisel çalışma planı</span></div></section><form className="interview-setup" onSubmit={start}><div className="setup-heading"><div><p className="eyebrow">01 · Kariyer yolu</p><h2>Interview track seç</h2></div><span>Seçimin soru dağılımını belirler</span></div>{tracks.length ? <TrackSelector tracks={tracks} selected={track} onSelect={setTrack} /> : <p className="loading-copy">Trackler yükleniyor…</p>}<div className="setup-divider" /><div className="setup-heading"><div><p className="eyebrow">02 · Seviye</p><h2>Başlangıç ayarları</h2></div><span>Seviye performansına göre uyarlanır</span></div><TopicSelector {...{ topic, setTopic, difficulty, setDifficulty }} />{error && <p className="error">{error}</p>}<button className="primary-action" disabled={busy}>{busy ? 'Görüşme hazırlanıyor…' : 'Mock interview başlat'} <span>→</span></button></form></main>; }
