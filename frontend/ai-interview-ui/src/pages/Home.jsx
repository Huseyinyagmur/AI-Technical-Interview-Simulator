import { useState } from 'react';
import TopicSelector from '../components/TopicSelector';
import { startInterview } from '../services/interviewApi';
export default function Home({ onStarted }) { const [topic, setTopic] = useState('C#'); const [difficulty, setDifficulty] = useState('Junior'); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const start = async e => { e.preventDefault(); setBusy(true); setError(''); try { onStarted(await startInterview(topic, difficulty)); } catch (err) { setError(err.message); } finally { setBusy(false); } }; return <main className="hero"><div><p className="eyebrow">AI Technical Interview Simulator</p><h1>Practice the interview, not just the theory.</h1><p>Answer five focused technical questions and get immediate feedback.</p></div><form className="card" onSubmit={start}><TopicSelector {...{ topic, setTopic, difficulty, setDifficulty }} />{error && <p className="error">{error}</p>}<button disabled={busy}>{busy ? 'Starting…' : 'Start mock interview'}</button></form></main>; }
