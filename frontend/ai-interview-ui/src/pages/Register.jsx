import { useState } from 'react'; import { register } from '../services/authApi';
export default function Register({ onAuth, onLogin }) { const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState(''); const submit=async e=>{e.preventDefault();try{onAuth(await register(name,email,password));}catch(x){setError(x.message)}};return <main className="auth-page"><section className="auth-shell"><aside className="auth-aside"><div className="auth-logo">AI</div><p className="eyebrow">AI Technical Interview</p><h2>Kariyer hedefin için<br />bugün pratik yap.</h2><div className="auth-aside-card"><span>✦</span><p>Tek bir hesapla tüm görüşme geçmişini ve gelişimini takip et.</p></div></aside><form className="auth-card" onSubmit={submit}><div className="auth-heading"><p className="eyebrow">Başlayalım</p><h1>Hesap oluştur</h1><p>Kişisel mülakat alanını birkaç saniyede hazırla.</p></div><label>Ad soyad</label><input placeholder="Adın Soyadın" value={name} onChange={e=>setName(e.target.value)} required/><label>Email adresi</label><input type="email" placeholder="ornek@email.com" value={email} onChange={e=>setEmail(e.target.value)} required/><label>Şifre</label><input type="password" minLength="6" placeholder="En az 6 karakter" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<p className="error">{error}</p>}<button className="auth-primary">Hesap oluştur <span>→</span></button><button type="button" className="auth-link" onClick={onLogin}>Zaten hesabın var mı? <b>Giriş yap</b></button></form></section></main>; }
